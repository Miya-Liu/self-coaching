# Finalised Review: `self-coaching` v0.3.1

## Executive Summary

| Dimension | Grade | Detail |
|-----------|-------|--------|
| Architecture | A | Clean adapter spine; one engine, one client (`CompositeClient`), many backends via `ORCHESTRATOR_*_BACKEND` env flags |
| Mock completeness | A | C01–C18 harness passes, golden fixtures in CI, 267 core tests green (3.10–3.12) |
| Live completeness | C+ | E-path + C06 sparse work; T-path blocked by C07 pipeline timeout → C12/C18 never reached |
| Documentation | B+ | `progress.md` is accurate; 3–4 peripheral docs lag behind shipped code |
| Production readiness | C | No Dockerfile, no sqlite, no canary/rollback, no async training POST |
| Housekeeping | B- | Build artifacts tracked, missing optional dep for supabase tests, no coverage tool |

**Bottom line:** Architecturally coherent, mock-proven, adapter-wired — but the live end-to-end proof (one full clock tick: pipeline batch → CLI train → AgentEvals holdout → promote) has not yet passed. Ship T1 skill pack today; next claim-worthy milestone is live Track 1 green.

---

## Completed Work (high confidence, CI-proven)

- **T1 skill pack** — v0.3.1, install scripts, Hermes deploy path, 5 skills
- **Mock loop** — `completeness: PASS` (C01–C18), one-command demo, HTTP + module transports
- **Loop driver refactor** — decomposed into `loop_config.py`, `scoring.py`, `e_path.py`, `t_path.py`
- **Self-learning (migration M2)** — mock + HTTP adapters, evolve API, loop wiring, CI split-stack
- **Self-questioning pipeline (migration M3)** — `PipelineServiceClient`, `SelfQuestioningPipelineEngine`, proceed gating, opt-in CI workflow
- **Self-tuning (migration M4)** — HTTP mock AERL (M4.0–M4.3), `CLITrainAdapter` via db_bridge/Supabase, Sprints 0–3
- **AgentEvals (migration M1)** — live holdout adapter, timeout knob, orchestrator integration
- **Coach shell (M5 partial)** — registry loader, `ClockScheduler` with per-agent locking, HTTP/WS service, `clock.py` tick driver, `agent_bridge_live.py` + `COACH_BRIDGE=agent`
- **Evolution engine (M1)** — `record-eval` → `check-drop` → `run` → gate → `deploy_manifest.json`
- **CI** — doctor, shellcheck, py_compile, pytest matrix, mypy, 10+ smoke scripts, contract sync check

---

## Live Smoke Reality (from your terminal run)

```
Preflight:  Pipeline ✓  Supabase ✓  AgentEvals ✓
Tick:       C06 sparse self-questioning ✓
            C07 batch self-questioning ✗ (120s timeout, proceed=False)
Audit:      C12 holdout ✗  C18 promote ✗
```

Root cause in `t_path_last.json`: pipeline job didn't complete within 120s → `batch_self_questioning_failed` → T-path never ran train → holdout gate never fired → no promote. Adapters are connected; the failure is operational (poll budget + real pipeline latency).

---

## Documentation Drift (needs reconciliation)

| Document | Claims | Reality |
|----------|--------|---------|
| `mock-to-real-migration.md` header | "M1 AgentEvals PASS" only | M2–M4 adapter work is done |
| Same doc §M4 "Remaining" | CLI adapter Sprint 0–2 open | `CLITrainAdapter` shipped; Sprints 0–3 done |
| `self-tuning-trainer-api-plan.md` §11 | M4.4 "not started" | Superseded by CLI path; implementation tracker marks done |
| `roadmap.md` M5 | "Replace MockCoachAgentBridge" unchecked | `agent_bridge_live.py` + `COACH_BRIDGE=agent` already exist |
| `docs/design/overall.md` | Rich architecture diagram | Not in `docs/README.md` index (unlinked) |

**Source of truth:** `docs/project/progress.md` — align everything else to it.

---

## Unresolved Tasks & Blockers

### P0 — Fix before claiming next milestone

| # | Task | Why it matters |
|---|------|----------------|
| 1 | **Fix C07 pipeline timeout** — increase poll budget, add scenario-specific timeout override, or allow dry_run proceed fallback | Blocks entire live T-path; without it, live Track 1 can never pass |
| 2 | **CT-D01 dataset handoff** — loop buffer → remote training YAML/JSONL | CLI train currently runs fixed remote config; live T-path promotion is symbolic without real data |
| 3 | **Doc consolidation pass** — reconcile migration doc, roadmap, trainer plan with `progress.md` | Three milestone namespaces (roadmap M0–M5, migration M0–M6, integration Phase 0–5) confuse readers |

### P1 — Operational quality

| # | Task | Why it matters |
|---|------|----------------|
| 4 | Add `supabase` optional dep (`[project.optional-dependencies]`) + pytest skip marks on LoRA/db_bridge tests | 10 tests error-collect on every fresh checkout; 5-minute fix |
| 5 | Add `pytest-cov` to `[dev]` deps + CI coverage gate | 267 tests but no visibility into uncovered paths |
| 6 | Update roadmap M5: mark `agent_bridge_live.py` as done, list remaining gaps (cron examples, per-agent coaching root validation, LLM proxy) | Roadmap says "not done" for something that exists |
| 7 | Decide fate of `.kiro/specs/mock-training-service-tinker/` — 7 detailed requirements for extended AERL mock, zero implementation | Backlog item that could mislead someone scanning the repo |

### P2 — Production path (roadmap M2–M4)

| # | Task | Notes |
|---|------|-------|
| 8 | Dockerfile / process model for T2 `serve` | Roadmap M2 |
| 9 | sqlite persistence (runs, idempotency, events) | Roadmap M2 |
| 10 | Async `POST /training/runs` + poll (202 + status GET) | Roadmap M2 |
| 11 | Canary deploy + rollback pointer | Roadmap M4 |
| 12 | Human approval gate in evolution engine | Roadmap M4 |
| 13 | Live metric watch + auto-rollback | Roadmap M4 |

### P3 — Nice-to-have cleanup

| # | Task |
|---|------|
| 14 | Remove or sunset `services/adapters/aerl_client.py` (deprecated alias wrapper) |
| 15 | Gitignore `.mypy_cache/` db files, `self_coaching.egg-info/`, `mock-services/ci-*` output dirs |
| 16 | Populate or remove `experience/RUN_SUMMARY.json` (null metrics placeholder) |
| 17 | Add `docs/design/overall.md` to the docs README index |
| 18 | Consider replacing sys.path manipulation with proper `src/` layout or namespace packages (long-term) |

---

## Completeness by Capability (Mock vs Live)

| Capability | Mock CI | Live | Gap |
|------------|---------|------|-----|
| E-path scoring + Σ routing | ✓ | ✓ | — |
| Self-learning (learn + evolve) | ✓ | mock backend in live scenario | Real staging learner not default |
| Sparse self-questioning C06 | ✓ | ✓ | — |
| Batch self-questioning C07 | ✓ | ✗ (timeout) | Pipeline latency > poll budget |
| Train (HTTP mock) | ✓ | n/a | Mock only |
| Train (CLI/db_bridge) | ✓ (smoke) | probe OK | Full T-path blocked by C07 |
| Holdout gate C12/C18 | ✓ | ✗ | T-path never reaches it |
| Promote / generation++ | ✓ | ✗ | Cascading from C07 |
| Orchestrator drop loop | ✓ | partial (eval live; train deferred) | — |
| Coach 24×7 scheduler | ✓ | mock bridge default | Live bridge opt-in, untested in production |

---

## Recommended Next Actions (ordered)

1. **Increase C07 poll timeout** in live scenario config (or pipeline-side: add a faster dry_run mode that the adapter can use for smoke)
2. **Run live Track 1 smoke until green** — C06 ✓, C07 ✓, C12 ✓, C18 ✓
3. **Consolidate docs** — one pass to align migration/roadmap/trainer-plan with `progress.md`
4. **Wire CT-D01** — loop buffer export → remote training input
5. **Add supabase optional dep + pytest marks** (quick win, removes noise)
6. **Add coverage reporting** (quick win, informs future test investment)
7. **Tag the state** — once live Track 1 passes, tag a release that represents "adapters proven"

---

That's the full picture. The repo is in good shape for what it claims to be (a validated mock platform with real adapter wiring). The critical path to "live-proven" is short but specific: C07 timeout → dataset handoff → holdout gate fires → promote succeeds.
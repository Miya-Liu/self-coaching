# Error log (Experience)

**Experience** — bugs and failures during runs. See `SKILL.md`.

Bugs, crashes, OOMs, failed commands, and integration issues **during** experiment execution.

Append a short block per incident; do not dump full `logs/*.log` content here—reference the file path and line range instead.

**Categories**: crash | oom | parse_error | env | logic_bug | other

---
